---
/** Bouton principal (compatible shadcn-vue Button quand installé) */
interface Props {
  href: string
  class?: string
}
const { href, class: className } = Astro.props
---
<a
  href={href}
  class:list={[
    'inline-flex h-11 items-center justify-center gap-2 rounded-md bg-[var(--primary)] px-8 text-sm font-medium text-[var(--primary-foreground)] shadow transition-colors hover:opacity-90',
    className,
  ]}
>
  <slot />
</a>
