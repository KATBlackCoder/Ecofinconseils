---
/** Hero page À propos (design React: gradient, pas full viewport) */
import AnimateHero from './AnimateHero.astro'

interface Props {
  title: string
  subtitle: string
}
const { title, subtitle } = Astro.props
---
<section class="bg-gradient-to-br from-[#0A1929] via-[#0D2137] to-black py-20 md:py-32">
  <div class="container mx-auto px-4 lg:px-8">
    <AnimateHero>
      <div class="mx-auto max-w-3xl text-center">
        <h1 class="mb-6 text-4xl font-bold text-white md:text-5xl">{title}</h1>
        <p class="text-lg text-gray-300">{subtitle}</p>
      </div>
    </AnimateHero>
  </div>
</section>
